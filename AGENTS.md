# AGENTS.md — maintainer brief for AI-assisted development

You (an AI model) are continuing development of **Plot Essential and Instructions Maker** (internal module id `loreAgent`; see the naming note below), a SillyTavern third-party extension built iteratively with the repo owner, who tests on **SillyTavern under Termux/Android, accessed from a mobile browser**. Read this whole file before changing anything. Its sibling project (same author, same patterns) is Continuity Copilot.

## What this extension is

A floating chat panel where the user talks to an LLM about a plain-text/markdown document, and the LLM edits the document via a strict protocol — one `<docedits>` JSON block per reply (find/replace, insert_after, append, replace_all, optional `"doc"` to target an attached reference document). Edits render as red/green diff cards the user approves; applies are undoable. Documents, presets, sessions, and undo stacks all live in `SillyTavern.getContext().extensionSettings.loreAgent` — **never** in chat or chatMetadata; the extension must work with no character/chat loaded.

## Deterministic document linter (v0.11.14)

`docLint(text)` scans the RAW document in code (no LLM) for inline double-spaces (runs of 2+ spaces between non-space chars, NOT leading indentation), trailing whitespace, tabs, and JSON validity; `collapseInlineSpaces` and `repairDocJson` (reuses `escapeRawControlsInStrings`) are the undoable fixes. `showDocLint` (the 🔍 Check button; floatWindow id `la_lintwin` — deliberately distinct from the `la_lint` button id, since floatWindow keys on element id) renders the report with spaces drawn as · and applies fixes via `commitDocChanges`. Rationale: LLMs cannot reliably perceive whitespace, so whitespace/JSON checks must be deterministic and never delegated to the model. All three functions are exported + test-covered.

## Internal naming — never change

The display name is **Plot Essential and Instructions Maker** (`manifest.display_name`). The **internal module id is `loreAgent`**, load-bearing in three places: the `extensionSettings.loreAgent` storage key (every saved document + preset lives here), the console `LOG` prefix `[LoreAgent]`, and the `globalThis.__loreAgentDebug` test/console export. Do **not** rename any of these — changing the storage key orphans all real user data, and the test suite + jsdom audit harness bind to `__loreAgentDebug`. The `la_` element-id prefix, `PRESET_WB_ID` (`seed_worldbook_maker`), and internal function names are likewise historical and stay. A rename touches display strings only.

## Architecture map (index.js, one IIFE, no imports)

1. Constants: `VERSION`, seeded presets, `DOCEDITS_PROTOCOL` (appended to every preset **in code** — never store it inside presets).
2. Helpers: `esc`, `uid`, `toast`, `copyText` (clipboard + execCommand fallback for http/LAN), `downloadText`/`mimeForName`.
3. Settings/data: `loadSettings` (merges defaults, seeds presets, migrates doc shapes), `ensureDocShape`, docs/presets/sessions accessors (`sess(doc)` = active session), `pushHistory` (80-entry cap/session), `pushUndo` (8 backups/doc, batch-tagged), `resolveDocByName`, `refsOf`.
4. Parsing: `findBlock` (LAST opening tag with a closer, prefer JSON-looking inner — prose mentions of the tag must never poison it), `parseDocEdits` (fence-strip, string-aware trailing-comma repair via `stripTrailingCommasOutsideStrings` — NEVER a blind regex, which mutated ", ]" inside string values — plus `escapeRawControlsInStrings` covering all C0 controls, per-edit `docName`), `stripBlocks` (same span logic for display), `splitThinking` (closed + unclosed think tags; since v0.12.0 it locates the docedits block FIRST and runs think extraction only OUTSIDE that span — this tool edits AI-instruction documents, so think-tags legitimately occur in prose and inside edit strings, and the old single-pass extraction could gut the block or swallow it into thinking).
5. Edit engine: `normChars` (1:1 length-preserving), `levenshtein` (works on word arrays), `locate` (exact → normalized → fuzzy word-window ±15%, sim ≥ 0.78, 3–150 words, **alignment-vote prefilter** so 30k+ char docs stay fast), `applyEditToText`, `applyEdits` (multi-doc batches), `undoLast` (batch-aware via `settings.batchLog`, per-doc fallback).
6. LLM: `callLLM` via `ConnectionManagerRequestService.sendRequest(profileId, messages, maxTokens, {stream, signal})`; streaming returns a generator — accumulate with `acc = t.startsWith(acc) ? t : acc + t` (chunks may be cumulative or delta); reasoning arrives via `chunk.state?.reasoning`. Fallback `generateRaw({prompt, systemPrompt})`. Stop = AbortController + `ctx.stopGeneration?.()`, keep partial text.
7. Prompt assembly: `buildMessages` = system (preset + protocol) → recent session history (`historyDepth`) → the **full document(s)** injected into the LAST user message (`[REFERENCE DOCUMENT: name]` blocks first, `[DOCUMENT: name]` last; recency improves verbatim copying). Documents are sent whole, never truncated. If the window contains NO user turn (possible after per-message deletes), the context blocks are appended as their own user message — the document must ALWAYS be in the request (v0.12.0). Since v0.14.2: older assistant replies are resent with their raw docedits block stripped (`stripOldProposalBlocks`, exported; the LATEST reply keeps its block verbatim as the anti-re-proposal reference, and `contextTokenBreakdown` mirrors the transform), and adjacent same-role messages are merged before sending — `[STATE]` notes share the user role with real turns, and strict-alternation providers (Bedrock-hosted Claude, some proxies) hard-400 on consecutive same-role messages.
8. Conversation ops: `send`, `runGeneration` (guards doc AND session switching mid-generation — since v0.12.0 a switched-away reply is SAVED into its originating session, never discarded, with no edits staged; parse errors and Max-tokens-truncated blocks are fed back to the agent as persistent [STATE] notes, the same self-heal loop as apply-failures; always re-syncs `pendingEdits` to the visible reply), swipes on the last assistant message, `retryLast`, edit-and-continue, per-message delete, sessions + `branchAt(idx)`.
9. UI: panel (stylesheet-based, ST theme vars), `showEditor` floating window and the generic `floatWindow(id, opts)` shell used by the worldbook manager + compare view (**all floating overlays 100% inline-styled — never move their styles to CSS**; each `floatWindow` id gets one Esc-to-close listener on first creation, guarded by the module `overlayStack` so Esc closes only the TOPMOST open overlay (v0.12.0); `anyFloatWinOpen()` gates the fullscreen Esc handler), `makeDraggable` (window-level pointer listeners — `setPointerCapture` is unreliable on Android WebView; drag zones: header, doc/session/ref bar, quick row), diff cards, wand-menu entry, `/lore` slash command, `init` with APP_READY + 3s fallback (guarded by `typeof document` so the node load test can't crash).

## Worldbooks (v0.8.0)

A worldbook is an ordinary document holding a JSON array of entry objects (`{name, keys[], content, strategy, order, comment}`), assigned to the seeded **Worldbook Maker** preset (`PRESET_WB_ID`). Its prompt (`WORLDBOOK_MAKER_PROMPT`) is a full working default, not a placeholder. The engine is pure and test-covered:
- `parseWorldbook(text)` — tolerant parse; accepts a top-level array, `{entries:[...]}`, or ST's `{entries:{"0":{...}}}` object-map; infers strategy from ST fields (`constant`→blue, bare `vectorized`→chain). Never throws. Numeric fields (`order`/`depth`/`probability`) are coerced via `numOr` so string-typed numbers (`"300"`) survive instead of silently resetting to defaults (v0.11.0 fix — `Number.isFinite` on a raw string is false).
- `serializeWorldbook(entries)` — inverse of `parseWorldbook` for the fields that matter: canonical JSON, required fields always emitted, optional fields only when non-default. Idempotent and round-trip-safe. Backbone of per-entry editing and Validate & repair.
- `estTokens(s)` / `worldbookTokenStats(entries)` — rough (~chars/4) token estimate; `total`, per-entry, and the always-on **blue** subtotal (the cost paid every message).
- `lintWorldbook(entries)` — non-blocking author warnings (green-without-keys, empty content, duplicate names).
- `worldbookToST(entries)` — emits ST World Info schema. Mapping: blue→`constant:true`; green→keyed + `selective:true` + `vectorized:true` (fires on keywords AND semantically when the user has vectors); chain→`vectorized:true`, no keys. Round-trips back through `parseWorldbook`.
- Per-entry fields (v0.9.0): entries also carry `order`, `position` (friendly `before_char`/`after_char`/`at_depth`), `depth`, `probability`. `normalizePosition`/`positionToST` convert to/from ST's numeric codes (0/1/4). The Worldbook Maker prompt teaches the model to choose each field from the entry's role (spine→before_char+high order, dossiers→after_char, live lore→at_depth; sets share an order). Exporter sets `useProbability` only when probability≠100 and `depth` only for at_depth. All round-trip through `parseWorldbook`.
- `docLooksLikeWorldbook(doc)` — true if WB preset or content parses as entries. Drives the 🌐→ST export button visibility and whether **View** opens the worldbook manager vs the plain text editor.
UI: **+WB** creates one (`[]` + WB preset); **View** on a worldbook opens the **worldbook manager** (`showWorldbookManager` → `wbRenderList`/`wbRenderEntryForm`, a `floatWindow`): per-entry card list with a real edit form (no hand-editing JSON), token-budget header, **Validate & repair** (`repairWorldbook`), Add/Delete, **From doc** import (`wbSourcePicker`), **Move →** promote an entry into another document (`wbPromoteEntry`, undoable across both docs via `commitDocChanges`), and a raw-JSON escape (`viewDocRaw`); **🌐→ST** exports (`exportWorldbookST`). PE↔worldbook interaction is via the existing 🔗 reference system + per-edit `"doc"` targeting + the manager's Move/From-doc actions — no separate sync layer. Keep keywords as the deterministic floor; vectors are always an additive bonus, never a single point of failure.

### Compare view (v0.11.0)
**⚖ Cmp** (`showCompare` → `renderCompareBody`, a `floatWindow`) shows 2–4 documents side by side, read-only, for cross-referencing drafts. Layout toggle: **columns** (horizontal scroll, 82vw panes for mobile swipe) vs **stacked** (vertical). Selection (`settings.compareIds`, capped at 4) and `settings.compareLayout` persist. Per-pane copy. **Compare→agent bridge (v0.11.1):** each pane has a 🔗 attach toggle that adds/removes that doc as a reference of the *active* doc (the active doc's own pane shows an `active` badge and can't self-reference); Compare itself is view-only — only 🔗 references are actually sent to the agent, and attaching live-syncs the panel's 🔗 count + reference bar.

### Cross-document undo (v0.11.0)
`commitDocChanges(changes, label)` applies text changes to N docs as one batch, pushed to the same `settings.batchLog` the main **Undo** walks — so a promote (append to target + remove from worldbook) reverts in a single Undo. Mirrors the commit tail of `applyEdits`.

### Live context meter (v0.11.2)
`contextTokenBreakdown(doc)` (pure, exported, test-covered) mirrors `buildMessages` exactly — doc/ref tokens come from `blockTokensFor` (pure length arithmetic, test-pinned equal to `estTokens(docBlock(doc))`), never from concatenating the full document per refresh (v0.12.0 perf fix) — system+protocol + document + 🔗 references (in full) + windowed history — and returns a per-component breakdown where `total = system + doc + refsTotal + history`. The header subtitle (`updateSub`, already called by `renderHistory` at generation end and every mutation) shows a live `~Nk ctx` readout via `kFmt`; tapping it pops a one-line breakdown toast with per-reference token counts. It is a ~chars/4 estimate of context *sent*, not the model's context-window limit. Related: `settings` now initializes to a valid default object at load (not `null`) so pre-init access and the node/jsdom harnesses can't NPE — `loadSettings` reassigns it to the ctx-backed object in production.

### Pending-proposal awareness + supersede (v0.11.5, provenance since v0.12.0)
`pendingEdits` is **document-scoped, not session-scoped** — a proposal's find/replace anchors are against the active document, which every session of that document shares. So session ops (`switchSession`, and therefore `newSession`/`branchAt`, plus `deleteSession`) must **not** clear `pendingEdits`. Since v0.14.2 the staging area is **persisted per document** (`doc.pendingEdits`, backfilled by `ensureDocShape`, carried by backup/restore): the module variable is always the ACTIVE doc's array, every reassignment must go through `setPendingEdits` (filter/concat produce new arrays and would detach the two), in-place status mutations must be followed by `persist()`, and the restore paths (`loadSettings`, `setActiveDoc` — which now RESTORES the newly-active doc's queue instead of clearing it) run `prunePendingEdits` to drop stamps pointing at a removed (session, message) and neutralize stamps whose session is gone. Since v0.12.0 every staged edit is stamped with its source (`fromSess` = session id, `fromMsg` = history index of the generating assistant reply; set in both staging paths of `runGeneration` and in `swipeAssistant`). Message-removal ops follow provenance instead of nuking: `deleteMessageAt`/`deleteLastExchange`/`retryLast`(splice)/`startEditUserMessage`/`clearConversation` all call the shared `spliceHistory` helper (v0.14.2), which wraps `adjustStampsForSplice` (pure, exported — drops exactly the edits whose source message was removed, shifts later stamps down) and then, if any dropped card had status `applied…` (its edits are still LIVE in the document — the undo batch survives), appends a replacement `[STATE]` note ("N edit(s) from the removed message(s) remain applied to the document — Undo to revert") so a retry can never re-apply blind on top of a committed change. The 80-entry rolling cap is deliberately exempt from the note (rolling window; document is the source of truth). `deleteSession` keeps the cards (v0.11.5 decision) but neutralizes their stamps (`fromSess=null, fromMsg=-1`) because `nextSessId` can reuse the id. The 80-entry history cap shifts stamps too (`appendToSession`, which adjusts the OWNER document's persisted array — a mid-generation-switch reply overflowing an inactive session must not leave that doc's stamps stale). Swipe navigation re-parses a swipe's edits but dedupes via `editIdentityKey` against edits already applied/skipped from that same (session, message) — this is the fix for the applied-append being resurrected as pending and double-applied. Since v0.12.2, staging a new reply's proposals also runs `autoSupersedeConflicts` (pure core `findAutoSuperseded`, exported): older still-pending proposals that PROVABLY conflict with an incoming one on the same target document are marked `superseded` mechanically — exact `editIdentityKey` duplicates (the re-ask case), two `replace` edits with the identical `find` (first-occurrence collision: they cannot both succeed in either order, and if the first's replacement contains the find, blind sequential application NESTS instead of failing), and any older pending edit on a doc an incoming `replace_all` rewrites. Deliberately NOT auto-resolved: insert-vs-replace sharing an anchor (insert doesn't consume it), an OLDER pending `replace_all` vs a newer targeted edit (superseding would silently discard wanted work — the v0.11.9 failure loop is the recoverable path), and rephrased finds (undecidable; the agent-side `<supersede>` tag remains the mechanism). All three staging sites call it: `runGeneration` fresh + swipe paths, `swipeAssistant` re-navigation. Do not "fix" a same-find collision by reordering applies — order cannot make both succeed; superseding the older is the invariant. At APPLY time (`applyEdits`, all buttons funnel through it), a second pre-pass (`resolveSpanConflicts`, pure + exported) locates every pending find-anchored edit against the document's current text and supersedes older edits whose spans provably interfere with a newer APPLICABLE one — this catches REPHRASED finds over the same passage, which staging cannot see. Geometry: consume-vs-consume overlap; older consume covering a newer insert anchor; older insert point strictly inside a newer span. Exempt: same batch, boundary touches, insert-vs-insert, appends, rewrites, non-locating edits. `resolveDocByName(docs, name)` is TWO-arg — pass the allowed list (`[doc, ...refsOf(doc)]`); a one-arg call returns null silently (the 0.12.2→0.12.3 fix). Cards are numbered "Edit N" by `pendingEdits` array position (stable — applied/skipped/superseded edits are marked, not spliced out). `formatPendingProposals(edits)` builds a `[PENDING PROPOSALS]` block (pending-only, same numbers) that `buildMessages` injects into the last user message (and `contextTokenBreakdown.proposals` counts) so the agent sees what it already proposed and references it instead of re-proposing. To revise a specific proposal the agent emits `<supersede>N</supersede>` (parsed by `parseSupersede` — tolerant of `1` / `1,2` / `Edit 1` / JSON / newlines); `runGeneration` marks those still-pending edits `status:'superseded'` (→ `statusCls` skip styling, excluded from `applyEdits`) **before** staging the new batch, so "Apply all pending" never applies a stale proposal beside its replacement. `stripBlocks` removes the tag from display. Both `parseSupersede`/`formatPendingProposals` are exported + test-covered; `__loreAgentDebug.getPendingEdits()` is a read-only inspection hook.

## Summaryception transplant mode (v0.14.0)

A fourth seeded preset, **Summaryception Auditor** (`PRESET_SC_ID` = `seed_sc_auditor`, prompt const `SC_AUDITOR_PROMPT`), plus a **+SC** button (`la_newsc` → `newTransplantDoc`, paste-first via `showEditor`) turn this extension into the editing surface for Summaryception **Memory Transplant** exports (`<!-- SC-TRANSPLANT/NOTEPAD/LEDGER/SNIPPET(+SC-DETAIL)/PIN -->` marker files).

- `SC_AUDITOR_PROMPT` is **derived from Summaryception’s `MEMORY_AUDITOR.md`** (same repo owner). Mandates (M-RECORD / M-EPISTEMIC / M-SCAN / M-EYE / M-TAGS) and commands (`*audit` `*fix` `*cleanup` `*optimize` `*brief`) carry over intact — they are environment-agnostic and shared ecosystem vocabulary; keep the names. The DELIVERY section is deliberately **rewritten for docedits**: no file redelivery, surgical marker-spanning edits, block insertion via a multi-line anchor ending at the previous block’s closer (bare `<!-- /SC-SNIPPET -->` closers repeat — a closer alone is never a unique find), `replace_all` only for `*optimize`/approved full `*cleanup` and then only with the COMPLETE document. If the upstream brief’s substance changes, re-derive; never re-introduce its chat-paste delivery rules here.
- `lintTransplant(text)` (pure, exported, test-covered) **mirrors Summaryception’s `parseTransplant` importer move-for-move**: case-sensitive `SC-` markers, payload must be brace-form JSON or absent, segment = opener → next known opener, first same-kind closer wins, missing closer runs to the next opener, broken/nameless `SC-LEDGER` payload or fieldless body drops the dossier, later duplicate ledger names/notepads overwrite earlier, empty snippet/pin bodies are dropped (a detail-only snippet loses its detail too). Detection is case-INsensitive on purpose while classification stays exact — a case-mangled marker is invisible to the importer, which is precisely why the linter must see it. **Cross-repo coupling:** if `parseTransplant` in the Summaryception repo changes tolerances, this linter must be re-mirrored in the same session. **Linearity invariant (v0.14.1):** `lintTransplant` runs on the UI thread against arbitrary pasted text — every scan in it must be O(n): the malformed-comment detector is an `indexOf` span walk with a persistent cursor, and line numbers come from a prebuilt newline index. Never reintroduce nested `[^>]*…[^>]*`-style regex scans (quadratic backtracking froze the main thread — the perf tests in `test.js` enforce wall-clock budgets and will fail the suite).
- `docLooksLikeTransplant(doc)` (preset id or marker sniff) gates a transplant section inside `showDocLint` (inventory + per-issue line/severity list); `newTransplantDoc` runs the same lint at paste time. Rationale (same doctrine as `docLint`): marker integrity is deterministic-checkable and must never be delegated to the model — the whole failure mode of external auditing is a mutilated marker that the importer silently tolerates by dropping data.
- `isSeedPreset(id)` is now `DEFAULT_PRESET_PROMPTS` membership (single source of truth), not a hardcoded id pair — the old list predated the Worldbook Maker, leaving WB fake-deletable (loadSettings re-seeds missing seeds every load) and excluded from Reset default. All four seeds are undeletable + resettable; user presets unaffected.

## Invariants — do not break these

- Third-party extension boilerplate: `manifest.json` + single-IIFE `index.js` using only `SillyTavern.getContext()`.
- Bump `VERSION` **and** manifest version on every change; version shows in the panel header, editor title, and console — it is the only proof against mobile browser caching. Tell the user to hard-refresh after updates.
- Overlays/floating editors: inline styles + explicit `position:fixed` geometry, reset on every open. No flex-centering with vh-sized boxes.
- Escape everything rendered via innerHTML; prefer `textContent`/createElement.
- One `running` flag gates every generation path; `finally` restores UI; Send morphs into a red Stop.
- Smart scrolling: only auto-scroll if the user was within ~60px of the bottom, measured BEFORE mutating.
- Parse errors in docedits = harmless note, never a crash or partial apply.
- No length caps on presets or documents anywhere.
- `applyEditToText` applies exact / quote-normalized matches, and **whitespace-only-safe** fuzzy matches. `locate` marks a fuzzy match `safe` iff the matched span equals the needle after collapsing whitespace (and quote/dash normalization) — i.e. identical words in the same order, only the spacing differs. Then applying the replacement changes nothing the model did not intend. Any word/punctuation difference (a model misquote) is `safe:false` and refused by applyEditToText — it fails cleanly and the agent re-quotes verbatim (it has the full document in context). This applies whitespace-collapse edits but NEVER writes an approximate match over authored text. Do NOT loosen `safe` to edge-word-match-only or to a sim threshold — an ~83% match can silently overwrite real words in an instruction file. History: v0.11.10 refused ALL fuzzy (too blunt — wrongly failed whitespace-only diffs at "100%"); v0.11.12 allowed edge-word-matched fuzzy (too loose — applied ~83% word-misquotes over real text); v0.11.13 = whitespace-only-safe (correct).
- Native browser widgets are suspect on this device: `<details>` failed to expand and `setPointerCapture` failed silently — always use explicit JS toggles/handlers with inline styles.

## Backup / restore (v0.13.0)
`buildBackupPayload` / `parseBackupPayload` / `mergeBackupIntoSettings` (all pure, exported, uid injected). Format tag `plot-essential-backup`, v1. Backups include docs + sessions + presets, EXCLUDE undo stacks. Restore is ADDITIVE ONLY — fresh doc ids, name suffixing, in-set ref remapping (dangling refs dropped), preset id/prompt divergence keeps both with the restored docs following the restored copy. Never add an overwrite/merge mode: the zero-data-loss guarantee is the feature.

## Concurrent instances share this checkout
More than one AI instance may have this SAME working tree mounted simultaneously (shared filestore), not just the same origin. Observed consequences: uncommitted edits from another instance appear in your tree mid-session, HEAD can advance under you, and version numbers you planned can be taken before you commit. Protocol: (1) before ANY push, `git fetch` and re-read `git log` — never assume HEAD is where you left it; (2) audit `git diff HEAD` hunk-by-hunk before committing — the working tree is shared state, not your private scratchpad, so authorship recall is not a verification method; (3) commit+push small coherent units promptly so the other instance rebases on reality; (4) if the tree contains coherent work you didn't author, treat it as a colleague's in-flight work: verify it against tests, never blindly revert; (5) version-bump at COMMIT time, not at the start of a work unit.

## Iteration protocol

- Before shipping: `node --check index.js` **and** `node test.js` (stubs `SillyTavern`, proves clean load including the 3s timer, runs the engine unit suite). Add tests for any new pure logic via the `globalThis.__loreAgentDebug` export.
- Beyond the engine suite, a **jsdom UI harness** (owner keeps it out-of-repo) boots the extension with a stubbed `SillyTavern` and drives the real wired UI (worldbook CRUD, entry editor, promote + cross-doc undo, compare, attach bridge, context meter) via simulated clicks, asserting live state through `__loreAgentDebug.getSettings()` (a read-only settings getter). It catches runtime/DOM errors the pure tests can't reach, but cannot verify real mobile CSS layout, touch drag, real toastr rendering, or a live generation round-trip.
- User reports bugs with screenshots; respond with **targeted patches, not rewrites**; update the README changelog every version.
- Data migrations (doc/session shape changes) must be lossless and test-covered — the user has real long-running data in extensionSettings.
- Owner's style: act and build immediately, terse communication, fix root causes, full automation with graceful degradation.
